import { defineMessages } from 'react-intl'

import { name as scope }  from '../package.json'

export default defineMessages({
  title: {
    id: `${scope}.title`,
    defaultMessage: 'ОФИЦИАЛЬНЫЙ ИНТЕРНЕТ-МАГАЗИН АКСЕССУАРОВ BMW В РОССИИ',
  },

  description: {
    id: `${scope}.description`,
    defaultMessage: 'BMW',
  },
})
