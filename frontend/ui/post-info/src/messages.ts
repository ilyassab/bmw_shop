import { defineMessages } from 'react-intl'

import { name as scope }  from '../package.json'

export default defineMessages({
  oneView: {
    id: `${scope}.one_view`,
    defaultMessage: 'просмотр',
  },
  twoViews: {
    id: `${scope}.two_views`,
    defaultMessage: 'просмотра',
  },
  fiveViews: {
    id: `${scope}.five_views`,
    defaultMessage: 'просмотров',
  },
})
