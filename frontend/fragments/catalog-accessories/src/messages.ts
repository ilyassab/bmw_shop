import { defineMessages } from 'react-intl'

import { name as scope }  from '../package.json'

export default defineMessages({
  accessories: {
    id: `${scope}.accessories`,
    defaultMessage: 'Аксессуары',
  },
})
