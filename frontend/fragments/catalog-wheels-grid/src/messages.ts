import { defineMessages } from 'react-intl'

import { name as scope }  from '../package.json'

export default defineMessages({
  wheels: {
    id: `${scope}.wheels`,
    defaultMessage: 'Комплекты колес',
  },
})
