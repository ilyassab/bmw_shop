import { defineMessages } from 'react-intl'

import { name as scope }  from '../package.json'

export default defineMessages({
  favorite: {
    id: `${scope}.favorite`,
    defaultMessage: 'Избранные товары',
  },
})
